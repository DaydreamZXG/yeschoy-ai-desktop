{
  "schemaVersion": 1,
  "contracts": [
    {
      "id": "tool-discovery-scan@v1",
      "kind": "query",
      "owner": "tool-config",
      "consumers": [
        "desktop-platform"
      ],
      "schemaLocator": "inline://contracts.json#/contracts/tool-discovery-scan@v1",
      "authorization": "Callable only by the bundled local desktop webview; it grants no account, credential, filesystem-write, installation, update, or network authority.",
      "idempotency": "The query is read-only. Repeating one request against the same executable paths yields an equivalent projection; the UI accepts results only for its newest requestId.",
      "compatibility": "Version 1 always returns the fixed five-tool catalog and the declared conservative statuses. New fields or status meanings require a new contract version.",
      "inputSchema": {
        "type": "object",
        "description": "One local read-only discovery request.",
        "properties": {
          "requestId": {
            "type": "string",
            "description": "Caller-generated correlation identifier used to reject stale UI results.",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z0-9_-]+$"
          }
        },
        "required": [
          "requestId"
        ],
        "additionalProperties": false
      },
      "outputSchema": {
        "type": "object",
        "description": "The complete bounded projection for the five V1 tools.",
        "properties": {
          "requestId": {
            "type": "string",
            "description": "The unchanged caller request identifier.",
            "minLength": 1,
            "maxLength": 64
          },
          "platform": {
            "type": "string",
            "description": "The platform family on which discovery ran.",
            "enum": [
              "windows",
              "macos",
              "linux",
              "unknown"
            ]
          },
          "startedAtEpochMs": {
            "type": "integer",
            "description": "Local monotonic correlation timestamp expressed as Unix milliseconds for freshness display.",
            "minimum": 0
          },
          "completedAtEpochMs": {
            "type": "integer",
            "description": "Completion timestamp expressed as Unix milliseconds and never earlier than startedAtEpochMs.",
            "minimum": 0
          },
          "tools": {
            "type": "array",
            "description": "Exactly one result for every V1 tool in stable product order.",
            "minItems": 5,
            "maxItems": 5,
            "uniqueItems": true,
            "items": {
              "type": "object",
              "description": "One truthful local tool-discovery result.",
              "properties": {
                "toolId": {
                  "type": "string",
                  "description": "Stable tool identifier.",
                  "enum": [
                    "claude",
                    "codex",
                    "opencode",
                    "pi",
                    "dsh"
                  ]
                },
                "displayName": {
                  "type": "string",
                  "description": "Beginner-readable official tool name.",
                  "enum": [
                    "Claude Code",
                    "Codex",
                    "OpenCode",
                    "Pi",
                    "DSH"
                  ]
                },
                "status": {
                  "type": "string",
                  "description": "Observed installation and executable outcome without inferred configuration health.",
                  "enum": [
                    "not_found",
                    "detected_unverified",
                    "probe_failed",
                    "probe_timed_out",
                    "multiple_installations"
                  ]
                },
                "version": {
                  "type": "string",
                  "description": "Exact normalized version output when available, otherwise an empty string.",
                  "maxLength": 256
                },
                "candidateCount": {
                  "type": "integer",
                  "description": "Number of distinct executable candidates found after canonical-path deduplication.",
                  "minimum": 0,
                  "maximum": 32
                },
                "locationHint": {
                  "type": "string",
                  "description": "A sanitized source hint such as PATH or common location, never a complete user path or OS username.",
                  "enum": [
                    "none",
                    "path",
                    "common_location",
                    "multiple"
                  ]
                },
                "compatibility": {
                  "type": "string",
                  "description": "Conservative automatic-configuration eligibility for this release unit.",
                  "enum": [
                    "not_applicable",
                    "unverified_read_only"
                  ]
                },
                "reasonCode": {
                  "type": "string",
                  "description": "Stable presentation reason with no raw command output or secret-bearing diagnostic text.",
                  "enum": [
                    "tool_not_found",
                    "exact_version_not_allowlisted",
                    "version_command_failed",
                    "version_command_timed_out",
                    "multiple_executables_found"
                  ]
                }
              },
              "required": [
                "toolId",
                "displayName",
                "status",
                "version",
                "candidateCount",
                "locationHint",
                "compatibility",
                "reasonCode"
              ],
              "additionalProperties": false
            }
          }
        },
        "required": [
          "requestId",
          "platform",
          "startedAtEpochMs",
          "completedAtEpochMs",
          "tools"
        ],
        "additionalProperties": false
      },
      "dataClasses": [
        "internal"
      ],
      "observability": {
        "correlation": "requestId is returned unchanged and shown in advanced local details only.",
        "audit": "RU-001 emits no persistent audit record; the current in-memory result is the sole local projection.",
        "metrics": "RU-001 sends no telemetry and records no persistent metrics; elapsed time is computed in memory for the current scan only."
      },
      "errorSemantics": [
        "An invalid requestId rejects the whole query before any probe starts.",
        "A missing executable is a successful not_found result rather than a command failure.",
        "A non-zero or empty version command becomes probe_failed for only that tool and raw stdout or stderr is not returned.",
        "A probe exceeding the bounded timeout is killed and becomes probe_timed_out for only that tool.",
        "More than one canonical executable becomes multiple_installations and no candidate is silently selected as compatible.",
        "Any detected version not backed by a verified exact-version allowlist is unverified_read_only."
      ],
      "requirementRefs": [
        "REQ-003",
        "REQ-012",
        "REQ-026",
        "REQ-028",
        "REQ-029"
      ],
      "evidenceRefs": [
        "E-USER-040",
        "E-USER-037",
        "E-CODE-014"
      ],
      "status": "specified"
    },
    {
      "id": "desktop-account-readiness@v1",
      "kind": "query",
      "owner": "account",
      "consumers": [
        "billing",
        "desktop-platform"
      ],
      "schemaLocator": "inline://contracts.json#/contracts/desktop-account-readiness@v1",
      "authorization": "Callable only by the bundled local shell. It grants no network, account, credential, wallet, payment, configuration, updater, telemetry, or server authority.",
      "idempotency": "The query is a deterministic compiled-capability projection. Repeating it in RU-002 returns the same backend_upgrade_required truth except for the caller requestId.",
      "compatibility": "Version 1 has exactly one runtime status, backend_upgrade_required. A later deployed account integration requires a superseding contract and release unit rather than changing this meaning.",
      "inputSchema": {
        "type": "object",
        "description": "One local account-capability readiness request.",
        "properties": {
          "requestId": {
            "type": "string",
            "description": "Caller-generated local correlation identifier.",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z0-9_-]+$"
          }
        },
        "required": [
          "requestId"
        ],
        "additionalProperties": false
      },
      "outputSchema": {
        "type": "object",
        "description": "A fail-closed RU-002 projection that prevents unavailable remote capabilities from appearing live.",
        "properties": {
          "requestId": {
            "type": "string",
            "description": "The unchanged caller request identifier.",
            "minLength": 1,
            "maxLength": 64
          },
          "schemaVersion": {
            "type": "integer",
            "description": "Projection schema version.",
            "enum": [
              1
            ]
          },
          "status": {
            "type": "string",
            "description": "The only truthful account capability state in RU-002.",
            "enum": [
              "backend_upgrade_required"
            ]
          },
          "networkAttempted": {
            "type": "boolean",
            "description": "Whether this projection attempted any account network request.",
            "enum": [
              false
            ]
          },
          "serverNamespace": {
            "type": "string",
            "description": "The future first-party namespace required before activation.",
            "enum": [
              "/api/desktop/v1"
            ]
          },
          "authorization": {
            "type": "string",
            "description": "Whether website device authorization is available in this client-only unit.",
            "enum": [
              "unavailable"
            ]
          },
          "accountSummary": {
            "type": "string",
            "description": "Whether real account balance and summary data is available.",
            "enum": [
              "unavailable"
            ]
          },
          "usage": {
            "type": "string",
            "description": "Whether real account usage data is available.",
            "enum": [
              "unavailable"
            ]
          },
          "pricing": {
            "type": "string",
            "description": "Whether a server-authoritative official-versus-actual price projection is available.",
            "enum": [
              "unavailable"
            ]
          },
          "recharge": {
            "type": "string",
            "description": "Whether a verified first-party recharge destination is available.",
            "enum": [
              "unavailable"
            ]
          },
          "comparisonFx": {
            "type": "object",
            "description": "Product-approved display context that is not a live rate or billing input.",
            "properties": {
              "usdToCny": {
                "type": "string",
                "description": "The product-approved fixed USD-to-CNY comparison display rate.",
                "enum": [
                  "6.75"
                ]
              },
              "purpose": {
                "type": "string",
                "description": "The sole permitted purpose of the fixed rate.",
                "enum": [
                  "display_only"
                ]
              },
              "liveRate": {
                "type": "boolean",
                "description": "Whether the fixed display rate is a live market exchange rate.",
                "enum": [
                  false
                ]
              }
            },
            "required": [
              "usdToCny",
              "purpose",
              "liveRate"
            ],
            "additionalProperties": false
          }
        },
        "required": [
          "requestId",
          "schemaVersion",
          "status",
          "networkAttempted",
          "serverNamespace",
          "authorization",
          "accountSummary",
          "usage",
          "pricing",
          "recharge",
          "comparisonFx"
        ],
        "additionalProperties": false
      },
      "dataClasses": [
        "public",
        "internal"
      ],
      "observability": {
        "correlation": "requestId exists only in transient local UI state.",
        "audit": "RU-002 creates no account or authorization audit record because it performs no remote or persistent account operation.",
        "metrics": "RU-002 records and uploads no account, billing, or readiness metric."
      },
      "errorSemantics": [
        "The projection never falls back to browser cookies, browser access tokens, a manually pasted PAT, generic NewAPI token CRUD, or public pricing interpretation.",
        "Unavailable balance, usage, price, model, and wallet data are represented as unavailable rather than zero or stale values.",
        "A future server becoming reachable does not silently activate RU-002; activation requires a superseding frozen contract and executable deployed integration evidence."
      ],
      "requirementRefs": [
        "REQ-030"
      ],
      "evidenceRefs": [
        "E-USER-041",
        "E-EXT-024",
        "E-CODE-021",
        "E-PROP-004"
      ],
      "status": "specified"
    },
    {
      "id": "guided-configuration-preview@v1",
      "kind": "query",
      "owner": "tool-config",
      "consumers": [
        "desktop-platform"
      ],
      "schemaLocator": "inline://contracts.json#/contracts/guided-configuration-preview@v1",
      "authorization": "Callable only by the bundled local desktop webview. It grants no network, credential, configuration-read, configuration-write, rollback, installation, updater, telemetry, account, billing, or production authority.",
      "idempotency": "The query is a pure deterministic projection. The same requestId, toolId, and lineId produce the same preview with no observable side effect.",
      "compatibility": "Version 1 contains the fixed five-tool and two-line preview catalog and always blocks apply. Enabling a model, credential, write, test, or new adapter requires a superseding contract version and frozen release unit.",
      "inputSchema": {
        "type": "object",
        "description": "One transient local configuration-preview selection.",
        "properties": {
          "requestId": {
            "type": "string",
            "description": "Caller-generated transient correlation identifier.",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z0-9_-]+$"
          },
          "toolId": {
            "type": "string",
            "description": "Stable selected tool identifier.",
            "enum": [
              "claude",
              "codex",
              "opencode",
              "pi",
              "dsh"
            ]
          },
          "lineId": {
            "type": "string",
            "description": "Stable selected product line identifier.",
            "enum": [
              "mainland_optimized",
              "global_accelerated"
            ]
          }
        },
        "required": [
          "requestId",
          "toolId",
          "lineId"
        ],
        "additionalProperties": false
      },
      "outputSchema": {
        "type": "object",
        "description": "A truthful preview-only projection that cannot mutate the machine.",
        "properties": {
          "requestId": {
            "type": "string",
            "description": "The unchanged caller request identifier.",
            "minLength": 1,
            "maxLength": 64
          },
          "schemaVersion": {
            "type": "integer",
            "description": "Projection schema version.",
            "enum": [
              1
            ]
          },
          "status": {
            "type": "string",
            "description": "The only RU-003 preview capability state.",
            "enum": [
              "preview_only"
            ]
          },
          "toolId": {
            "type": "string",
            "description": "Stable selected tool identifier.",
            "enum": [
              "claude",
              "codex",
              "opencode",
              "pi",
              "dsh"
            ]
          },
          "displayName": {
            "type": "string",
            "description": "Beginner-readable official tool name.",
            "enum": [
              "Claude Code",
              "Codex",
              "OpenCode",
              "Pi",
              "DSH"
            ]
          },
          "lineId": {
            "type": "string",
            "description": "Stable selected product line identifier.",
            "enum": [
              "mainland_optimized",
              "global_accelerated"
            ]
          },
          "lineName": {
            "type": "string",
            "description": "Beginner-readable selected line name.",
            "enum": [
              "大陆优化",
              "全球加速"
            ]
          },
          "rootUrl": {
            "type": "string",
            "description": "The exact product-approved root URL for the selected line.",
            "enum": [
              "https://yeschoy.com",
              "https://api.yeschoy.com"
            ]
          },
          "protocolEndpoint": {
            "type": "string",
            "description": "The documented protocol endpoint preview, or an empty string when withheld.",
            "maxLength": 256
          },
          "endpointStatus": {
            "type": "string",
            "description": "Whether the endpoint is a documented preview or withheld as unverified.",
            "enum": [
              "documented_preview",
              "withheld_unverified"
            ]
          },
          "targetFile": {
            "type": "string",
            "description": "Sanitized documented target file hint, or an empty string when withheld.",
            "maxLength": 256
          },
          "ownedFields": {
            "type": "array",
            "description": "Documented fields a later verified adapter may own; never values read from the machine.",
            "minItems": 0,
            "maxItems": 12,
            "uniqueItems": true,
            "items": {
              "type": "string",
              "description": "One sanitized configuration field name.",
              "maxLength": 128
            }
          },
          "model": {
            "type": "object",
            "description": "Truthful model-selection availability for this client-only unit.",
            "properties": {
              "status": {
                "type": "string",
                "description": "The prerequisite that prevents a real model selection.",
                "enum": [
                  "server_catalog_required"
                ]
              },
              "modelId": {
                "type": "string",
                "description": "Always empty because no server model catalog is available.",
                "enum": [
                  ""
                ]
              }
            },
            "required": [
              "status",
              "modelId"
            ],
            "additionalProperties": false
          },
          "apply": {
            "type": "object",
            "description": "Fail-closed apply readiness and named prerequisites.",
            "properties": {
              "status": {
                "type": "string",
                "description": "The only permitted apply state in RU-003.",
                "enum": [
                  "blocked"
                ]
              },
              "blockers": {
                "type": "array",
                "description": "Every missing prerequisite for safe automatic configuration.",
                "minItems": 4,
                "maxItems": 5,
                "uniqueItems": true,
                "items": {
                  "type": "string",
                  "description": "One stable missing-prerequisite code.",
                  "enum": [
                    "desktop_backend_required",
                    "server_model_catalog_required",
                    "secure_credential_helper_required",
                    "exact_version_allowlist_required",
                    "dsh_web_adapter_required"
                  ]
                }
              }
            },
            "required": [
              "status",
              "blockers"
            ],
            "additionalProperties": false
          },
          "networkAttempted": {
            "type": "boolean",
            "description": "Whether the preview attempted any network request.",
            "enum": [
              false
            ]
          },
          "configurationRead": {
            "type": "boolean",
            "description": "Whether any existing configuration content was read.",
            "enum": [
              false
            ]
          },
          "configurationWritten": {
            "type": "boolean",
            "description": "Whether any configuration content was written.",
            "enum": [
              false
            ]
          },
          "credentialAccessed": {
            "type": "boolean",
            "description": "Whether any credential source was accessed or executed.",
            "enum": [
              false
            ]
          }
        },
        "required": [
          "requestId",
          "schemaVersion",
          "status",
          "toolId",
          "displayName",
          "lineId",
          "lineName",
          "rootUrl",
          "protocolEndpoint",
          "endpointStatus",
          "targetFile",
          "ownedFields",
          "model",
          "apply",
          "networkAttempted",
          "configurationRead",
          "configurationWritten",
          "credentialAccessed"
        ],
        "additionalProperties": false
      },
      "dataClasses": [
        "public",
        "internal"
      ],
      "observability": {
        "correlation": "requestId exists only in transient local UI state.",
        "audit": "RU-003 writes no configuration receipt or persistent audit record because it performs no operation.",
        "metrics": "RU-003 records and uploads no preview, tool, line, model, credential, or configuration metric."
      },
      "errorSemantics": [
        "An invalid requestId, toolId, or lineId rejects the pure projection without any side effect.",
        "DSH returns an empty protocolEndpoint and targetFile with endpointStatus withheld_unverified instead of guessing an adapter contract.",
        "The model remains server_catalog_required and an empty modelId is never rendered as a real model.",
        "Every projection blocks apply and keeps all four side-effect flags false.",
        "No future server availability or newly installed tool silently upgrades this contract into a write-capable flow."
      ],
      "requirementRefs": [
        "REQ-004",
        "REQ-008",
        "REQ-013",
        "REQ-026",
        "REQ-031"
      ],
      "evidenceRefs": [
        "E-USER-042",
        "E-EXT-025",
        "E-EXT-026",
        "E-EXT-027",
        "E-EXT-028",
        "E-CODE-022",
        "E-PROP-005"
      ],
      "status": "specified"
    }
  ]
}
