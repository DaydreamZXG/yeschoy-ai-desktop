{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "ru001-read-only",
  "description": "RU-001 exposes only Tauri core defaults and the bundled read-only discovery command.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
