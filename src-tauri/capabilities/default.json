{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "ru005-bounded-client",
  "description": "RU-005 exposes Tauri core defaults plus bundled read-only tool discovery and fixed-host DNS and TCP-443 diagnostics.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
