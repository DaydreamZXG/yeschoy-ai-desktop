# 野菜API 桌面助手 · UI 重设计交接（2026-09-12 第二轮）

> 上一轮交接：`野菜API-Trae接手交接-2026-09-12.md`。本轮完成双主题 UI 重设计落地（浅色纸感 + 深色工作台）。

## 本轮已完成

### 1. 设计稿（用户已确认）
目录 `/Users/zxg/Documents/trae_projects/yecai-ui-redesign/`，本地打开即可预览：
- `direction-a-dark.html` — 深色工作台（琥珀金 `#e8a33d`）
- `direction-b-light.html` — 浅色纸感（朱红，已修正为 `#c93e22`）
- `setup-page.html` / `models-pricing.html` — 接入页/模型价格页排版设计（**未落地**，见遗留）

### 2. 双主题色板落地（已生效）
CSS 架构：`index.css`（基座色板 + 组件基础）→ `tokens.css`（语义化扩展）→ `workbench-v2.css`（业务页样式）

- `src/index.css`
  - `:root` 浅色纸感：`--bg/--sidebar: #f7f6f3`，`--accent: #c93e22`，`--ink: #1a1b1e`，`--muted: #5f5e58`，`--faint: #6b6a64`，`--accent-soft: #fbeae6`
  - `:root[data-theme="dark"]` 深色工作台：`--bg: #0d0e10`，`--surface: #17191c`，`--accent: #e8a33d`，`--ink: #eceef0`，`--muted: #a3a8ae`，`--faint: #8b929b`，`--accent-soft: #292116`（实心 hex，见约束）
  - 清理 8 处硬编码蓝/绿：`.primary-action`、侧边栏/页面渐变、`.sign-in-card/.sign-in-art`、`.configuration-app-icon` 等，全部改 `var(--accent)` 系 + `color-mix`
  - **深色残留修复**：`[data-app="pi|dsh_web|hermes|openclaw"]` 图标原硬编码浅底色（`#eaf6ef` 等），已加 `[data-theme="dark"]` 覆写（品牌色与 surface color-mix，字色提亮）
- `src/tokens.css`（新建）：`--line-soft`、`--line-strong`、`--on-accent`（深主题按钮深字 `#20170a`）、`--font-display`
- `src/main.tsx`、`scripts/qa/daily-use/preview.tsx`：引入顺序插入 `tokens.css`
- `src/workbench/workbench-v2.css`：删除两处重复色板 `:root` 块（token 归口基座）；`.selection-warning`、`.primary-action` 改走 token；阴影/遮罩中性化

### 3. 验证状态
- ✅ `pnpm typecheck` 通过
- ✅ 对比度：用测试同款算法复算（见下"环境坑"），**30/30 组合 ≥ 4.5**（最严：浅色 faint/accent-soft = 4.65，深色 = 5.05）
- ✅ fixture 预览双主题渲染确认无破损：`pnpm vite --config scripts/qa/daily-use/vite.config.mjs` → http://127.0.0.1:4197/
- ⚠️ `pnpm test:unit` 未跑通（vitest 环境挂起，与改动无关，需正常环境补跑）

## 关键约束（改 CSS 必读）
`src/workbench/Workbench.test.tsx` 有两个 CSS 测试（只解析 `src/index.css`）：
1. 必含 `--font-ui:`、`--font-mono:`、`data-theme`、`button:focus-visible`、`prefers-reduced-motion`、`min-width: 320px`、`max-width: 420px`、`tabular-nums`、`overflow-wrap: anywhere`；禁 `Georgia|Times New Roman|@import|fonts.google`
2. 对比度：取 `:root` 和 `:root[data-theme="dark"]` 首个块，`--xxx: #hex;` 正则提取（**rgba 会解析失败 → NaN → 挂**），`ink/muted/faint` × `bg/surface/subtle/sidebar/accent-soft` 全部 ≥ 4.5

## 遗留任务（按优先级）
1. **补跑完整测试**：vitest 在此 worktree 挂起（见环境坑），换正常环境跑 `pnpm test:unit`
2. **接入页/模型价格页视觉重绘**：按 `yecai-ui-redesign/setup-page.html`（01 选模型分组 → 02 网络线路 → 03 确认接入 三视觉分组）与 `models-pricing.html` 落地，token 已就绪，只改 `workbench-v2.css` 排版层
3. **一键接入仪式感动效**（用户有意但暂缓，预算原因）：需求 = 点击"一键接入"(`.setup-apply`) 后按钮缩小 → 成功时仪式感动画。入口 `src/configuration/ConfigurationPreviewView.tsx:2117`，样式 `index.css:2520` `.setup-apply`。注意必须包 `@media (prefers-reduced-motion: reduce)` 降级（测试约束）

## 环境坑（省时间必读）
- **vitest 2.1.9 在此 worktree 启动即挂起**（RUN banner 后卡死，CPU 空烧）。已用零项目代码 probe 测试证明与业务改动无关；同一二进制在 /tmp 正常。绕过法：用 node 直接复算测试算法验证（对比度测试算法很简单，luminance 公式 + 4.5 阈值）
- **shell `rm` 对 worktree 路径被沙箱拦截**（不在 allowlist）；删文件用 IDE 文件工具（DeleteFile），不要 `rm -rf`
- **浏览器 MCP 不稳定**：`browser_click/take_screenshot/evaluate` 经常超时或 WebView not ready；vite dev server 本身正常，必要时让用户自己开 http://127.0.0.1:4197/ 看
- 主题切换机制：`document.documentElement.dataset.theme = "dark"`，本地存储 key 见 `src/workbench/appearance.ts` `APPEARANCE_KEY`

## 设计决策备忘
- 朱红 `#d9482b` 白底对比度 4.29 不达标 → 微调 `#c93e22`（4.58）
- 深色 faint 设计稿 `#6b7076` 在 surface 上仅 3.52 → 提亮 `#8b929b`（5.6）
- 深色 `--accent-soft` 必须是实心 hex（测试正则只认 `#hex;`）：用 `#292116` ≈ rgba(232,163,61,0.12) 叠 `#0d0e10`
- `--on-accent` 解决深主题琥珀金按钮上白字对比度不足（白字仅 2.16 → 深字 7.9）
