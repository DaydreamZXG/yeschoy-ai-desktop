# 同一运行周期内重新接入卡死：限定排障范围

用户报告：已接入成功，Codex 与野菜保持运行，换模型后再次一键接入，助手无法点击。当前没有该设备原始日志，不能断言全部此类卡死都来自同一原因。

基线：`1c77a96b0ed5cc48ee053a66c8f4bd41c409f867` 加本轮开始时已有未提交修复；保留原改动。不操作本机已安装应用、真实账户、用户配置、网关、服务器，不打包、不上传。

代码观测：`ConfigurationPreviewView.tsx` 在原生返回 `application_running` 后打开共享 `ConfirmDialog`。后者使用 Radix `DialogContent`，依赖 `fixed`、`left-1/2` 等 Tailwind 工具类。当前 `main.tsx` 只引入手写的 `index.css` 与 `workbench-v2.css`，没有 Tailwind 生成入口；正式 CSS 中没有这些定位规则。Radix 仍会禁用弹窗外的点击，因此应验证“弹窗在视口外而页面被锁住”的路径。

目标：复用现有共享弹窗所有者与 Radix 焦点／模态语义，补全与当前主题匹配、显式引入的必要样式。不引入第二套弹窗框架或另一套全局 Tailwind reset，不改变退出授权、模型选择、计费和接入事务。

测试范围：真实组件与正式样式的隔离浏览器页面；同一次挂载中的首次接入、换模型、提示保存、取消、确认后关闭遮罩、再次接入；浅色／深色及窄窗口；确认未同意时不发送重启授权。测试只用合成状态和模拟原生接口。

允许源码范围：`src/components/ui/dialog.tsx`、其组件样式及回归测试，必要的 `ConfirmDialog.tsx` 样式类、`DailyUse.test.tsx` 流程回归，以及本目录证据。其他源码维持原字节。

治理限制：已读取 govern-product-build 及必需参考，运行仓库审计和 RU-076 review；报告 73 项 RU-075 旧测试快照与当前源码／审计文件不相等。严格冻结流程未通过。按上层技能失败时采用最佳可用方法继续的指示，保留失败，不编辑旧证明或生成虚假冻结状态；只按上述原组件边界排障与修复，不宣称正式治理通过、整包已验收或生产就绪。
