# 野菜API 下载源部署验收

日期：2026-09-06。范围：RU-044 下载源基础部署，不是客户端自动升级上线。

## 已验证

- `https://ergou.qzz.io/health.json` 公开访问与指定源 IP 的直连访问均为 200，并通过正常 TLS 校验。原来的 Cloudflare 525 已消失；源站成功取得 Let's Encrypt 证书。
- 公开域名与直连源站各完成 11 项 HTTP/TLS 检查：健康响应、两个未发布更新通道、完整文件、Range、缺失文件、隐藏路径、原始/编码穿越路径、目录拒绝、写入方法拒绝。
- 分段下载响应 206、`Content-Range: bytes 8-39/189`，32 字节与本地探针对应切片完全一致。完整探针 SHA256 为 `aff684b2f45e557d88a8f4b7619ce5727612890b63170354f517c787c782474b`。
- 原始穿越路径在源站为 404，在 Cloudflare 公网入口被提前以 400 拒绝；其他拒绝语义按实际结果保留，没有把错误改成成功。
- HTTP 入口为 308 跳转到 HTTPS；HEAD 健康响应为 200。
- 新增服务为非 root、只读容器，没有绑定宿主机端口，仅挂载专用配置与公开目录；没有写入或读取账户 API Key。备份目录权限 0700、备份文件 0600。
- 共享入口只追加指定域名，容器启动时间前后均为 `2026-08-15T03:28:26.377449982Z`，没有重建或停止。既有三个网站路由的 200/200/301 状态前后保持一致。
- 本地两个验收测试覆盖配置边界、重放、重复/损坏片段、候选被改、共享文件被改、权限/inode 保留和受控回退。正式机器验收以 `.product-governance/execution/reports/` 中 RU-044 报告为准，不以本文代替。

## 当前发布状态

公开目录只有健康文件和无敏感数据的下载探针。`/updates/stable.json` 与 `/updates/beta.json` 都返回 204、`Cache-Control: no-store`，不声称已有可安装更新。

没有上传当前内部安装包，没有生成或发布更新签名，没有转存 Codex/Claude 安装包，没有改变客户端或重新打包。

下一步需完成野菜客户端更新公钥/插件接入、递增版本、签名更新产物和 macOS/Windows 实际升级回归；第三方下载另行核对来源、许可、签名和完整依赖。中国大陆多运营商无代理速度未验证。

## 运维交接

- 配置和源目录见 `deploy/download-origin/README.md`。
- 逐项机器观测、配置 SHA256 与前后状态见 `deploy/download-origin/deployment-receipt.json`。
- 已保留原入口备份，回退必须先检查当前入口仍为本次版本，不能覆盖他人的后续修改。
- 未修改服务器 SSH 密码、密钥、DNS、Cloudflare 加密模式或防火墙。口令没有保存到项目文件或下载目录；建议用户尽快更换已在对话中提供的口令，并迁移为 SSH 密钥登录。

本次使用 govern-product-build 将基础设施部署与客户端更新/第三方分发分开验收，保留此前产品工作的状态，不以源站上线代表整个发布链已完成。
