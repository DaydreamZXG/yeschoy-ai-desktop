# 野菜API 桌面客户端 · 批次 4 完成交接 — 2026-09-13

> 前序：`野菜API-批次3交接-2026-09-13.md`（批次 3 基线）、`野菜API-功能问题修复计划-2026-09-13.md`（批次定义）。
> 本批范围：修复计划批次 4（信息架构与一致性 P1）—— **#9 工具清单单源**、**#10 「高级工具」更名**、**#11 高级模式折叠区（PRD 6.8）**、**#20 configured 判定分层**、**#22 断电恢复横幅**。
> 用途：批次 4 收尾记录 + 下一批（批次 5 · 价格口径 P1）启动基线。开工前先对照 `野菜API-PRD条款追踪矩阵.md`。

---

## 一、结论速览

| 任务 | 状态 | 说明 |
|---|---|---|
| #20 `configured` 判定过严 | ✅ 完成 | 接入页状态分层：`connectionEstablished`（connected/changed/legacy 视为已接入），选择有变化时保持次级按钮+连接卡，不强制回退一级「一键接入」 |
| #9 工具清单三处不一致 | ✅ 完成 | `appCatalog.ts` 唯一展示源（WORKBENCH_APPS + COMING_SOON_APPS）+ 强校验 5 断言；hermes/openclaw 展示层摘除，OpenCode「即将支持」 |
| #10 「高级工具」名不副实 | ✅ 完成 | 导航/入口四语言更名「已安装的工具」；`bundled_only`/`path_precedence` 确认经 `yeschoyDiscovery.selection.*` 业务文案呈现，无裸枚举泄漏 |
| #11 高级模式折叠区（PRD 6.8） | ✅ 完成 | 设置页「高级信息」`<details>` 折叠区：实际 Base URL / 配置文件+Finder reveal / 实际模型 ID / 密钥指引 / 最近写入+最近请求 / 诊断复制 |
| #22 断电恢复无全局通知 | ✅ 完成 | 首页顶部 `recovery-banner`（role=alert）+「前往恢复」一键进入修复 |
| 验证 | ✅ 完成 | typecheck ✅；前端 vitest `/tmp/yeschoy-vitest` 全量 **1414/1414** ✅；Rust 本批零改动，维持批次 3 的 259/259 基线 |
| 收尾 | ✅ 完成 | 追踪矩阵 §6.2/§6.8/§8 已翻转；PRD §6.8 密钥条款按客户端自治回写 |

---

## 二、各任务要点

### #20 `configured` 判定（接入页）

- [ConfigurationPreviewView.tsx](../src/configuration/ConfigurationPreviewView.tsx)：新增 `connectionEstablished`，与 `configured` 同源守卫（`applying` 不展示；存在 activation 结果时须 `resultIsCurrent`，防跨账户冒用）。`connection-action-deck` / `OpenConnection` / 按钮样式改判 `configured || connectionEstablished`。
- 顺带修复 `applyFlash` 泄漏：条件失效（如外部切换账户/选择）时显式 `setApplyFlash(false)`。
- 测试：DailyUse 新用例覆盖「接入后选择变化保持次级提示 + 打开使用可用」「从未接入保持一级」。

### #9 工具清单单源

- [appCatalog.ts](../src/workbench/appCatalog.ts)：`WORKBENCH_APPS`（claude_desktop / codex_desktop / dsh_web / pi / claude_code，PRD §3.2 定案）+ `COMING_SOON_APPS`（opencode，仅展示）。
- [appCatalog.test.ts](../src/workbench/appCatalog.test.ts)：5 项强校验（合法激活 ID、不含遗留工具、即将支持不占激活 ID 空间、协议层非 V1 仅限遗留工具、只读发现必须包含即将支持应用）。
- [App.tsx](../src/App.tsx)：发现页展示过滤 `V1_DISCOVERY_TOOLS`（协议层 TOOL_CATALOG 保持 Rust 契约不动）；`AppLibraryView` 渲染即将支持卡片；四语言 `yeschoyDiscovery.comingSoon`。
- **遗留处置**：hermes/openclaw 适配器代码移出随批次 6 #26，不在本批删。

### #10 「高级工具」→「已安装的工具」

- [copy.ts](../src/workbench/copy.ts)：`advanced` / `advancedAction` / `moreApps` 四语言更新（moreApps 同步摘除爱马仕/小龙虾提法）。
- 四语言 locale `yeschoyCandidate.nav.tools` 同步（该节点当前无代码引用，仅为防漂移）。
- 测试同步：Repair.test 按钮名、App.test 折叠区数量（**#9 遗留断言漂移**：`查看其他 5 个应用` → `3 个应用`，上批 584 子集未覆盖 integration 文件，本批全量跑出后修复）。

### #11 高级模式折叠区（PRD 6.8）

- 新组件 [AdvancedConnectionDetails.tsx](../src/settings/AdvancedConnectionDetails.tsx)，挂在 `SettingsView` 设置账本区末尾，默认折叠。
- 数据全部客户端自治：Base URL = `CONFIGURATION_LINES.rootUrl + CONFIGURATION_TOOLS.protocolSuffix`（preview.ts 静态目录）；配置文件 = targetFile + 复用现有 `open_config_folder` 命令 Finder reveal（claude→`claude`、codex_desktop→`codex`、claude_desktop→`claude-desktop`、pi→`pi`；dsh_web 无本地配置）；最近请求复用 `RecentRequest` 业务语言渲染；诊断复制含读取编号。
- [connections.tsx](../src/configuration/connections.tsx)：控制器新增 `requestId`（Controller 类型中可选，兼容既有测试替身）。
- 四语言 `yeschoySettings.advanced.*`；样式 token 化（workbench-v2.css）。
- **范围决策（已回写 PRD §6.8 原文）**：密钥掩码/创建时间属服务端数据，按客户端自治原则改为「密钥存于系统安全存储 + 恢复原设置即撤销」指引，矩阵记「部分实现」。

### #22 断电恢复横幅

- [AppLibraryView.tsx](../src/workbench/AppLibraryView.tsx)：启动读取到 `recovery_pending` → 首页顶部 `workbench-notice`（role=alert）+「前往恢复」→ `onOpenSetup(toolId, "repair")`。
- 测试：DailyUse 新用例断言横幅文案与跳转参数。

---

## 三、验证与环境

- **typecheck**：`npx tsc --noEmit` ✅（worktree 内）。
- **前端全量**：vitest 在 worktree 内仍永久挂起（沙箱限制，零 import 探针也挂），继续使用 `/tmp/yeschoy-vitest` 最小拷贝环境跑全量：**164 文件 / 1414 用例全绿**。改动文件均以 `cp` 同步过去。
- **Rust**：本批零 `src-tauri` 改动，未重跑 cargo（基线 259/259）。
- **真机手测待做**：Finder reveal 实际打开、`<details>` 折叠交互、密钥指引文案现场确认。

---

## 四、矩阵与 PRD 回写

- `野菜API-PRD条款追踪矩阵.md`：§6.2 断电恢复全局通知 → 已实现；§6.8 七行全部翻转（密钥条目「部分实现」并注明范围缩减）；§8 断电启动恢复补横幅说明。
- `野菜API-桌面客户端-V1-PRD.md` §6.8：密钥条款由「掩码、创建时间和撤销入口」改写为客户端自治版本（2026-09-13 定案），两边一致无漂移。

---

## 五、批次 5 启动基线（价格口径 P1）

按修复计划批次 5 执行，开工前先读矩阵对应行：

- **#12** 每百万人民币字段直显（ModelsView/接入摘要），「1 亿 Token 费用参考」降级折叠，启用 copy.ts 死文案。
- **#13** 「查看全部模型」开关：不兼容模型置灰+原因；空态区分「无兼容模型」vs「数据未返回」。
- **#14** 汇率 6.75 三处硬编码收敛为服务端 `comparisonFx` 单点。
- **#15** 幽灵组件处置（CostComparison / ServiceCatalogPanel / CandidateHomeView / readiness 占位）：逐组件接线或删除，收尾时执行「这个文件还有人引用吗」检查。

> 注意本批经验：跨层估算（前端全量含 integration 1414 条，不是 584 子集）；断言与单源目录强耦合的用例（折叠区数量、按钮名）在清单增删后必须全量回归。
