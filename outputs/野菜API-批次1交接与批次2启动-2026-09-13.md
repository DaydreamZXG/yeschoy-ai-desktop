# 野菜API 桌面客户端 · 批次 1 完成交接 + 批次 2 启动 — 2026-09-13

> 输入：`野菜API-功能问题修复计划-2026-09-13.md`（批次 1/批次 2 章节）、`野菜API-PRD条款追踪矩阵.md`（本批已更新）。
> 用途：批次 1（信任与安全）收尾记录 + 批次 2（模型能力元数据专项 M1~M6）启动基线。下一轮迭代开始前先对照追踪矩阵。

---

## 一、批次 1 完成记录（信任与安全，5/5）

| # | 任务 | 结果 | 关键文件 |
|---|---|---|---|
| #3 | 恢复设置询问 Key 撤销（PRD 9.1 明文） | 确认弹窗加独立勾选项「同时撤销此应用的专用 Key」（默认勾选、可取消）；原生按选项决定 `revoke_owned_tool_tokens`；撤销失败提供重试入口 | `src/configuration/RestoreConnection.tsx`、`src-tauri/src/tool_activation.rs` |
| #8 | 余额不足引导（PRD 6.2） | 常量单点 `LOW_BALANCE_THRESHOLD_CNY = 5` + `balanceAlert()`（只判 CNY）；≤¥5 首页+接入页充值横幅；≤0 接入按钮旁提示**不阻断**（写入设置免费） | `src/account/finance.ts`、`src/workbench/LowBalanceBanner.tsx`（新）、`AppLibraryView.tsx`、`ConfigurationPreviewView.tsx` |
| #24a | 钱包打开失败静默 | `useWalletRecharge` 消费 `openWallet` 返回值，失败 toast + 重试；App 全局挂载 Sonner `Toaster`（随主题） | `src/workbench/useWalletRecharge.ts`（新）、`App.tsx`、`AccountView.tsx` |
| #16 | 切换工具静默联动线路 | 仅当联动发生且线路不同时弹 toast「已切换到此应用上次使用的线路」；手动选择不弹 | `ConfigurationPreviewView.tsx:684` |
| #6 | 401 双场景（客户端自治消解） | 不改服务端：401/403 统一「登录失效」，四语言文案覆盖撤销/过期双场景——「登录已失效。若你刚在网站撤销过此设备，请检查设备安全；否则重新授权即可。」 | `src/workbench/copy.ts`（`accountSessionExpired`） |

**配套改动**：`workbench-v2.css`（横幅/耗尽提示样式，全部走 token：`--danger-soft`/`--danger`/`--space-4`）、`scripts/qa/daily-use/native-fixture.ts`（`?mode=lowbalance` 余额 3.20 / `?mode=depleted` 余额 -0.50 两态）、`finance.test.ts`（新）、`DailyUse.test.tsx`（+4 用例）、`Savings.test.tsx`（+2 用例）。

**追踪矩阵已翻转 7 行**：§5 已撤销、§5 敏感账号操作、§6.2 提醒行（保留有偏差，仅剩批次 6 #18 模型下线检测）、§6.2 充值按钮、§6.4 线路确认、§9.1 撤销设备、§9.1 删除配置询问 Key。

**验收状态**
- typecheck 通过（exit 0）。
- `balanceAlert` 逻辑用 Node 24 类型剥离直接跑真实源码（`node /tmp/validate-finance.ts`），9/9 断言全绿：阈值上/下/恰等、零、负数、非 CNY、未定义、畸形金额。
- React 用例断言与四语言文案、`formatMoney` 实际输出逐一核对一致（`¥3.20` 精确匹配）。
- **遗留（环境限制，非代码问题）**：worktree 内 vitest 2.1.9 启动即挂（已知，`/tmp` 下同二进制正常）——`pnpm test:unit` 全量需在正常环境跑一次；#3 勾选/不勾选两态建议真机过一遍。
- 预览：http://127.0.0.1:4197/ （Vite 热更新；如已关闭，重启后加 `?mode=lowbalance` / `?mode=depleted` 验证横幅两态）。

---

## 二、批次 2 启动基线（模型能力元数据专项，M1~M6）

**目标**：让「1M 上下文、图片输入、思考档位、最大输出」成为单一事实源驱动、UI 可见的模型能力声明。

**现状四层断裂**（修复计划 2026-09-13 调查，勿重复勘察）：
1. 服务端 `AccountModel` 无能力字段（PRD 6.5 已规划由版本化模型目录补充，V1 随客户端发布）。
2. `src/model-profiles/catalog.json`（**活目录**，新 UI 唯一消费）：仅 20 模型 × 3 家；Claude 全系缺 `input`/`maxOutputTokens`；gpt-5.4-mini/5.3-codex/5.2 缺三字段。
3. `src/config/piModelCatalog.ts`（**死目录**，只喂遗留 Pi 表单）：57 模型 × 21 家，字段最全但滞后（无 glm-5.3）。
4. 两份目录 ID 格式不一致：`anthropic/claude-opus-4.6`（点分）vs `claude-opus-4-6`（连字符）。**合并键空间 = 线上 `/api/pricing` 实际下发模型 ID**，逐条人工核对命名差异。

**任务清单**（详见修复计划批次 2 表）：

| # | 要点 | 关键约束 |
|---|---|---|
| M1 | 目录合并补全：piModelCatalog 57 条迁移进 catalog.json，按线上清单对账；四字段（contextWindow/input/maxOutputTokens/reasoningLevels）必填；官网优先，二手权威来源需在 sources 注明；`verifiedAt` 更新 | 新增 schema 校验脚本：四字段必填、ID 对账（未收录允许缺席但列出清单）、sources 可解析为 URL |
| M2 | `profile.ts` 新增只读 `modelCapabilities(id)` | 注释保持「不是白名单」定位；**未收录返回空，不猜能力**（PRD 6.5 明文）；单测命中/未命中/字段缺失三态 |
| M3 | ModelsView/ModelPicker 能力徽标行：`1M 上下文`（≥1000000）、`图片输入`、`思考 N 档`、`最大输出 128K` | 字段缺失**不显示该徽标**（不标"不支持"）；i18n 四语言；不撑破长模型 ID 换行（沿用 overflow-wrap 修复）；对比度复跑 |
| M4 | 删除 piModelCatalog.ts（536 行）+ piThinkingProfiles.ts | 删前确认 Rust 侧 Pi 适配器不依赖；删后 typecheck + 原生测试 |
| M5 | catalog.json 托管官网静态文件（`yeschoy.com/desktop/model-catalog.json` + sha256），客户端拉取、内置兜底 | **不改 NewAPI**（客户端自治）；对账脚本进 CI；拉取失败兜底路径单测 |
| M6 | 服务端中文 `description` 是否渲染为模型卡片副标题（决策） | **隐私约束**：不得渲染私有上游描述——既有测试 `keeps raw model descriptions off both model surfaces` 必须保持绿；只渲染经审核公开说明 |

**验收**：模型页对 gpt-6-astra 显示「1M 上下文 · 图片输入 · 思考 5 档 · 最大输出 128K」；Claude 全系图片徽标可见；kimi/qwen/doubao/glm 等国产模型能力声明与线上清单一致；对比度 30/30 复跑通过。

**建议实施顺序**：M1（数据先行，含对账脚本）→ M2（查询 API）→ M3（UI 徽标）→ M6（决策小项，可与 M3 同批）→ M4（删除死目录）→ M5（CDN 托管，依赖 M1 对账脚本）。

---

## 三、环境注意事项（延续，每批必读）

- **vitest 在 worktree 启动即挂**（`.codex/worktrees/c485`，零 import 探针也挂；同二进制在 /tmp 正常）。纯逻辑验证用 Node 24 类型剥离：`node xxx.ts` 直接跑真实源码 + 内联断言（本批 `balanceAlert` 即此法）。
- **shell `rm` 被沙箱拦**：删文件用 DeleteFile 工具。
- **对比度测试** `Workbench.test.tsx` 只解析 `src/index.css` 的 `#hex`；新样式一律用 `var()` 引用既有 token（不新增硬编码组合），token 本身 30/30 ≥4.5 已达标。
- 浏览器 MCP 截图偶发超时：优先 `browser_evaluate` 直接断言 DOM。
- 样式约定：颜色/间距/圆角/阴影全部走 token，不硬编码；动效带 `prefers-reduced-motion` 降级。
- i18n：新文案必须四语言（zh/zh-TW/en/ja）同步进 `copy.ts` 或 locales。

## 四、决策红线（勿漂移）

- 客户端自治：不为配套能力要求 NewAPI 服务端改造；依赖一律消解为客户端路径。
- 未收录模型**不猜能力**（PRD 6.5 明文）——徽标缺失即不显示，不标"不支持"。
- 工具范围 V1 = Claude Code/Codex/Pi/DSH web；OpenCode「即将支持」；Hermes/OpenClaw 已移出。
- 不发送计费测试消息（已回写 PRD）。
- PRD 是第二事实源：行为级决策必须回写 PRD 原文 + 翻转追踪矩阵状态。
