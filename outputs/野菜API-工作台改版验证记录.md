# 野菜API · 工作台改版验证记录

日期：2026-08-31。基线：`b73e8f6cb86b9aaac20c71aa01768eb24becccb0`。

## 本次交付

将已确认的 Cockpit 风格方向落实到现有客户端渲染层：固定侧栏、账户摘要、入门步骤、桌面应用卡片、模型与价格、用量账单及设置。统一字体、间距、图标、卡片、按钮和状态样式；提供浅色、深色、跟随系统三种外观。使用本机系统无衬线字体，模型 ID 使用等宽字体；不依赖在线字体。

原有原生只读识别、公共模型目录、线路诊断和接入预览继续使用原接口与校验。没有用设计样例余额、调用记录或模拟登录填充产品。

## 功能与安全边界

- 不修改服务器、NewAPI、Nginx、原生后端、API 契约或依赖清单。
- 不获取、保存或展示 API Key；不读取网站 Cookie 或用户应用配置。
- 授权登录、余额、真实账单、账户实际价格、充值与配置写入仍未接通，界面明确展示不可用状态。
- 模型 ID 保留完整展示；价格不从公开倍率推算，6.75 仅作为已确认的固定对比汇率。
- 外观只保存 `light`、`dark`、`system` 枚举；本机存储失败时仍可在当前会话切换。
- 识别失败或系统不支持时不展示零应用；多个安装按应用类型计数，卡片保留冲突说明；发现应用不等于接入成功。
- 未打包、未签名、未公证、未上传 GitHub 或发布新安装程序，既有安装包没有更新。

## 验证结果

- `pnpm run typecheck`：通过。
- `pnpm run build:renderer`：通过，产出最新 `dist`。保留现有浏览器数据库过期与较大 JS 分块警告，未为消除警告改动依赖或扩大本次范围。
- 定向 Vitest：8 个文件、43 项测试全部通过；其中工作台新增 13 项，覆盖真实组件渲染、未知/不支持/多安装/部分扫描、陈旧响应丢弃、导航与禁用写入、完整模型选择与线路清理、主题持久化/失败回退、四种语言字段、键盘焦点与颜色对比度。
- 实际 CSS 两主题的正文/辅助文字在页面、卡片、侧栏和强调底色上的测试组合均达到 4.5:1；这不是完整无障碍合规认证。
- `git diff --check -- src tests`：通过。
- 浏览器实际构建检查：1280 像素桌面浅色/深色、1000 像素中等窗口及 320 像素窄窗口；概览、应用接入、模型、用量账单、设置和诊断均检查。英文设置、模型及诊断在 320 像素窗口的页面宽度与内容宽度均为 320，无横向溢出。
- 只读独立复核发现的三个问题均修复：不支持/多安装误计为零、辅助文字对比度不足、换页焦点丢失。回归测试覆盖全部三项。

浏览器没有 Tauri 原生桥，因此浏览器中的桌面应用卡片如实显示“状态未知”；本次浏览器截图不构成 macOS/Windows 原生安装识别或端到端验收。既有真实接口使用通过源码及 mock 边界组件测试核对，不据此宣称账户服务已可用。

测试命令：

```sh
pnpm exec vitest run src/workbench/Workbench.test.tsx src/candidate/readiness.test.ts src/diagnostics/contract.test.ts src/configuration/preview.test.ts src/service-catalog/contract.test.ts src/service-catalog/access-plan.test.ts src/service-catalog/ServiceCatalogPanel.test.tsx src/service-catalog/handoff.test.ts --exclude 'release/**'
```

排除 `release/**` 是为了避免测试发现阶段递归遍历既有 DMG 暂存目录里的 Applications 符号链接，不排除上述任何目标测试。

## 验收依据

本次产品范围由 DEC-054 / REQ-038 确认；RU-013 仅为 RU-012 的验收执行器兼容性更正，不增加功能或发布权限。仓库使用 pnpm，而默认 Vitest 验收适配器要求 npm lockfile；更正后由只读隔离中的 pytest 场景调用现有真实 Vitest 测试，并验证非空机器报告、选择文件、全通过且无跳过，不修改 package.json 或 lockfile。

当前验收运行：`RU-013/WP-RU013-WORKBENCH@2026-08-31T13:37:08.888449+00:00`。最终机器结果与源码哈希以 `.product-governance/execution/reports/` 中该运行对应报告为准；本记录不替代机器验收。

历史记录格式修复：`野菜API-桌面优先改版验证记录.md` 仅恢复原验收快照中的末尾空行，使其哈希与历史证据一致，未改变历史结论。该历史空行是全仓 `git diff --check` 的已知格式提示；本次源码与测试无空白错误。

这是一轮客户端界面实施与验证，不是完整商业 V1 或公开发布批准。下一步可在用户确认外观后单独进行原生启动验收与候选安装包重建。
