# 野菜API 桌面助手 · 阶段三（体系化第一批）交接 — 2026-09-13

> 前序：`野菜API-UI重设计交接-2026-09-12.md`（阶段一止血 + 阶段二动效/品牌）。
> 本批范围：审计报告阶段三 5 项中的 3 项 —— **#10 死媒体查询清理、#13 文案收尾、#14 面板切换过渡 + 骨架屏**。
> 未做：#11 CSS 架构合并、#12 设计 token 化（见"下轮范围"）。

---

## 一、本批改动清单

### #10 窄窗死媒体查询清理（窗口形态已决策：1120×760 / min 860 不变）

删除 11 块、约 580 行与 min-width 860px 矛盾的 ≤760px 死代码：

| 文件 | 删除块 | 约行数 |
|---|---|---|
| src/index.css | max-width: 680 / 720 / 420 | 371 |
| src/workbench/workbench-v2.css | max-width: 640×2 / 760 / 720 / 480 | 146 |
| src/billing/comparison.css | max-width: 640 | 23 |
| src/installation/installation.css | max-width: 720 | 17 |
| src/components/ui/dialog.css | max-width: 480 | 5 |

- 同步删除 `html, body, #root { min-width: 320px }` 死语义（index.css）。
- **保留的活查询**：index.css 1100 / 900 / pointer:coarse / reduced-motion；v2 min-1450 / 1020 / reduced-motion。
- **测试断言修正**：`Workbench.test.tsx` 响应式 token 断言 `min-width: 320px / max-width: 420px` → `max-width: 900px / max-width: 1100px`（活断点）。

### #13 文案收尾（zh.json + 汇率单点化）

- **人称**：您 → 你，14 处全量替换。
- **省略号**：`...` → `…`，66 处（已确认只在 value，key 无三连点）。
- **引号统一为 “”**：「」→“” 15 处；`\"{{var}}\"` → `“{{var}}”` 全量（JSON 代码示例不受影响）；叙述引用 4 处逐条修正。JSON 代码示例/Placeholder 内的 `\"` 保留。
- **汇率单点化**：5 处 6.75 硬编码收编到 `COMPARISON_FX`（`src/account/readiness.ts`）：
  - `src/billing/copy.ts` 四语言 methodBody → `{fx}` 占位；
  - `src/billing/CostComparison.tsx` import COMPARISON_FX，渲染处 replace + 明细行 `1 USD = {COMPARISON_FX.usdToCny} CNY`。
  - 有意保留：`comparison.ts:101` 的 `z.literal("6.75")`（服务端数据 schema 校验层）与 readiness.ts 常量本身（单点真相源）。
- zh.json 改动后 `node -e require` 解析验证通过。

### #14 面板切换过渡 + 骨架屏（index.css 动效区新增约 90 行）

- **面板切换 `view-enter`**：`.workbench-page / .configuration-workspace / .diagnostics-workspace / .settings-workspace / .workspace` 统一 fade + translateY(4px→0)，200ms ease-out。
  - 条件渲染的视图（home/account/models/diagnostics/settings/tools）每次切换重新挂载自动播放。
  - keep-alive 的 `.persistent-setup` 从 `hidden` 恢复 display 时动画自动重放（CSS 盒重建行为，无需 JS）。
  - **决策偏差**：报告方案含"旧视图 fade-out 120ms"，本批只做 enter。out-in 需要延迟卸载状态机，与 persistent-setup keep-alive 架构冲突，风险大于收益；如后续要补，在 App.tsx 加 120ms 延迟切换层。
- **骨架屏体系**：
  - `.skeleton` 基类（opacity pulse 1.4s，报告允许的两个循环动画之一）+ `.skeleton-heading / .skeleton-line(.is-short) / .skeleton-value` 变体；
  - 落点 1：`AccountView.tsx` 账户加载卡 —— LoaderCircle 换成 3 条骨架（heading + line + line.is-short），原文案移入 `.sr-only` 供屏幕阅读器；
  - 落点 2：`Savings.tsx` 三处金额 dd loading 时加 `skeleton skeleton-value`（"—" 文本保留在 DOM，`Savings.test.tsx` 断言不受影响）。
- **顺手修复**：全局补 `.sr-only` 定义 —— `SessionManagerPage.tsx` 两处 sr-only span 此前**无任何样式、辅助文本直接可见**，属既有 a11y bug，现已随定义落地修复。

---

## 二、验证状态

| 项 | 状态 |
|---|---|
| `pnpm typecheck` | ✅ 通过（exit 0） |
| zh.json 合法性 | ✅ node require 解析通过 |
| 浏览器活体（http://127.0.0.1:4197/ fixture） | ✅ home/models/setup 三视图 `view-enter 0.2s` 计算样式命中；skeleton `skeleton-pulse 1.4s` 背景/圆角正确；962px 视口渲染无破坏；控制台无错误 |
| `pnpm test:unit` | ⚠️ **未跑** —— 本 worktree vitest 启动挂起是已知环境坑（与改动无关），需换正常环境补跑 |
| reduced-motion | 走全局拍平规则：view-enter/skeleton-pulse 动画清空后，骨架留静态色块、视图直接呈现，无信息丢失 |

## 三、给下一位的坑与约定

1. `Workbench.test.tsx` 只 `readFileSync("src/index.css")` 做文本断言，且对比度测试 regex 只认 `#hex;`（rgba/变量会 NaN fail）—— 动效/骨架新规则只用了 transform/opacity/color-mix 背景，不踩雷。
2. vitest 2.1.9 在本 worktree 零 import 探针也挂起；CSS 对比度类测试可在正常环境复跑。
3. 沙箱拦 shell `rm`，删文件用 DeleteFile。
4. 预览：`scripts/qa/daily-use` 的 vite fixture（127.0.0.1:4197），热更新即时生效。
5. 动效性能白名单：仅 transform/opacity 动画；background/border-color 仅 ≤200ms transition；同屏循环动画 ≤2（当前：skeleton-pulse + setup-apply 进度条）。

## 四、下轮范围（阶段三剩余）

- **#11 CSS 架构合并**：index.css（现约 3600 行）只留 token + 元素基础，组件规则归 workbench-v2.css；删约 50 组双文件同名覆盖与派生死规则。**大重构、用户不可见，需全量视觉回归**。
- **#12 设计 token 化**：`--space-* / --radius-* / --shadow-* / --z-*` 阶梯，渐进替换 447 处奇数 px、14 档圆角、10+ 处硬编码阴影（含绿调 #12332615 两处重复）、z-index 1/12/20/40/50/60/110（popover 20 低于 dialog overlay 40 的潜伏冲突）。
- 可选补做：面板切换旧视图 fade-out 120ms（见上方决策偏差）；en/zh-TW/ja 三个 locale 的人称/标点同规则过一遍（本批只做了 zh.json）。
- 阶段二遗留：正常环境补跑 `pnpm test:unit`。
