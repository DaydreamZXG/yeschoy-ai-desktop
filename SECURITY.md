# 野菜API 安全策略

## 支持范围

仅当前最新公开版本接收安全更新。内测包、旧版本和从非官方来源取得的安装包不在支持范围内。

## 安全边界

野菜API 是本地桌面助手，会在用户确认后修改所选 AI 应用的配置，并在本机 loopback 地址启动协议适配服务。以下边界属于安全范围：

- 发往本机 HTTP 服务的请求，包括认证、请求体大小、并发、超时和协议转换。
- 模型、计费分组和账户接口返回的不可信数据。
- OAuth 回调、会话令牌、API Key、系统安全存储以及日志中的敏感信息。
- 应用配置的备份、恢复、原子替换和并发修改。
- 第三方应用发现、下载、厂商签名校验和启动。
- 野菜自身的构建、代码签名、公证、更新签名、下载清单和发布流程。

客户端只应把本机 `127.0.0.1` 服务暴露给已接入的应用；它不得依赖模型的自我介绍判断真实路由。接入成功必须来自实际请求结果，失败不得覆盖用户原设置。更新域名 `ergou.qzz.io` 只保存公开构件、签名和清单，不保存账户、模型或用户配置。

## 报告漏洞

请勿在公开 Issue 中提交密钥、访问令牌、配置文件、服务器地址详情或可直接利用的漏洞步骤。请通过 [GitHub Security Advisories](https://github.com/DaydreamZXG/yeschoy-ai-desktop/security/advisories/new) 私下报告，并提供：

- 受影响版本和操作系统；
- 不可信输入从哪里进入、经过哪些组件；
- 最小复现步骤和实际影响；
- 已做脱敏的日志或截图。

我们会尽量在 48 小时内确认、7 天内给出初步判断。修复会通过 [项目 Releases](https://github.com/DaydreamZXG/yeschoy-ai-desktop/releases) 发布；客户端只接受由内置公钥验证通过、且版本严格递增的更新。

## 不属于漏洞的情形

- 用户主动选择并确认的普通本地文件操作；
- 用户自己编写并启用的命令或扩展按其设计执行；
- 没有可复现路径的扫描器输出；
- 只影响已停止支持的旧版本。

如果上述行为可由远程数据、导入文件、OAuth 回调或本地 HTTP 请求在未获知情同意时触发，仍属于安全问题。
